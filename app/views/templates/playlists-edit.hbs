<h3>Edit Playlist</h3>

<input class=name value="{{playlist.name}}" />

<table>
  {{#each playlist._songs}}
    <tr>
      <td><textarea>test</textarea></td>
      <td><a href="/{{year}}/{{month}}/{{day}}/{{slug}}">{{title}}</a> - {{album}}</td>
    </tr>
  {{/each}}
</table>

<button class=save>Save</button>
