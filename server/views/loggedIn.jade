extends layout

block content
  header#header-container.header-container
  .container-fluid.outer-container
    #page-container.page-container
  footer
    .queue-container
    .player-container
    .progress-container
      .progress-bar
      .position-bar
    .buttons
      .bar.bar-left
        .last
      .pause
      .bar.bar-right
        .next

  ul#notifications
  script(type='text/javascript')
    // bootstrap user and csrf objects
    var user = !{JSON.stringify(user)}
    , csrf = !{JSON.stringify(csrf)}
    , years = !{JSON.stringify(years)}
    , shows = !{JSON.stringify(shows)}
    , songs = !{JSON.stringify(songs)}
  if isProduction
    script(type='text/javascript',src='/javascript/app.min.js')
  else
    script(type='text/javascript',src='/javascript/vendor.js')
    script(type='text/javascript',src='/javascript/app.js')

  script(type='text/javascript')
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

      ga('create', 'UA-39615507-1', 'jit.su');
